CONTROLPLANE_ADDR=:8080
PG_DSN=postgres://postgres:postgres@localhost:5432/agent_builder_platform?sslmode=disable

TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_NAMESPACE=agents
TEMPORAL_TASK_QUEUE=agent-builder-platform

# Worker secrets (v1 model from TRD)
AGENT_SECRET_OPENAI_DEFAULT=replace-me
AGENT_SECRET_BILLING_TOKEN=replace-me

# Example DB connection DSN env var names for db nodes.
AGENT_DB_ZENDESK_RO_DSN=postgres://readonly:readonly@localhost:5432/zendesk?sslmode=disable
